define(["hl/fetch", "hl/rest", "hl/invent", "hl/move", "hl/dismiss"],function(fetch, rest, invent, move, dismiss) {
  return {
    Fetch: fetch,
    Invent: invent,
    Rest: rest,
    Move: move,
    Dismiss: dismiss
  };
});
