define(["ml/fetch", "ml/invent", "ml/move", "ml/rest", "ml/stand"], function(fetch, invent, move, rest, stand) {
  return {
    Fetch: fetch,
    Invent: invent,
    Move: move,
    Rest: rest,
    Stand: stand
  };
});
